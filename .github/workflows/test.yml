name: test

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash -n apply.sh test.sh herdr/setup.bash herdr/apply.sh shell/opencode.bash
      - run: ./test.sh --repo-only
